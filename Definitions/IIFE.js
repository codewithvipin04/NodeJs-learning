/* Immediate Invoked Function Expression(IIFE) in NodeJs ?

   1. Before the module's code is executed, nodejs will wrap it with a 
      function wrapper that provides it's scope.
   2. this save us having worry about conflicting functions or variables.
   3. there is proper encapsulation and reusability is unaffected.  
*/